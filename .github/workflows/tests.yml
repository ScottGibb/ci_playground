name: Tests
on:
    push:
        branches:
            - main
jobs:
    tests:
        runs-on: ubuntu-latest

        steps:
            - name: Testing
              run: |
                  echo "Running tests"
            - name: Fail
              run: |
                  echo "Failing"
                  exit 1
